- if @lessons.present?
  %table.table
    - @lessons.each do |lesson|
      %tr
        %td
          %strong= link_to lesson.title, lesson
        %td= (strip_tags lesson.body).html_safe
- else
  .none_present
    -if @course.present?
      :markdown
        No lessons exist yet. Be the first to [create one](#{new_lesson_path(course_id: @course.id)})...