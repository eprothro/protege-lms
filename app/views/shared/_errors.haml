- if object.errors.any?
  - message ||= "prohibited this #{object.class.model_name.humanize} from being saved:"
  #error_explanation
    %h2= "#{pluralize(object.errors.count, "error")} #{message}"
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg