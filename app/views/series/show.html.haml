%p#notice= notice

.pull-right.btn-group
  = link_to 'Edit Series', edit_series_path(@series), class: 'btn'
  = link_to 'Delete Series', @series, :confirm => 'Are you sure?', :method => :delete, class: 'btn'

%h2.page-header= @series.title

.description.well
  = @series.description.html_safe

%h3 Courses in This Series
= render partial: 'courses/list', locals: {:'@courses' => @series.courses}
%br
= link_to 'Add a Course', new_course_path(series_id: @series.id), class: 'btn'