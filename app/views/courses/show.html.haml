%p#notice= notice

.pull-right.btn-group
  = link_to 'Edit Course', edit_course_path(@course), class: 'btn'
  = link_to 'Delete Course', @course, :confirm => 'Are you sure?', :method => :delete, class: 'btn'

%h1.page-header
  %span.course_number= "[#{@course.number}]" if @course.number
  %span.course_title= @course.title

.muted.pull-right
  :markdown
    _This course is part of '#{link_to @course.series, @course.series}'_

.well.clear-right
  %p= @course.description.html_safe

%h3 Lessons in this Course
= render partial: 'lessons/list', locals: {:'@lessons' => @course.lessons}
%br
= link_to 'Add a Lesson', new_course_lesson_path(@course), class: 'btn'